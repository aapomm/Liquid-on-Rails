### Liquid on Rails

#### To run this demo on your local dev env:

```
$ git clone https://github.com/MattBudz/Liquid-on-Rails.git
$ bundle install
$ yarn install
$ bin/rails db:setup
$ bin/rails s
```
